<nav class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-4">
      <div class="flex justify-between h-16">
        <div class="flex">
          <a href="/" class="flex-shrink-0 flex items-center text-2xl font-bold text-purple-600">FocusHub</a>
        </div>
        <% if user_signed_in? %>
        <div class="flex items-center space-x-4">
          <%= link_to "Log out", destroy_user_session_path,
            data: { turbo_method: :delete, confirm: "Are you sure?" },
            class: "px-4 py-2 bg-transparent border border-purple-600 text-purple-600 rounded-4xl hover:bg-purple-50 transition" %>
        </div>
        <% else %>
          <div class="flex items-center space-x-4">
            <%= link_to "Sign up", new_user_registration_path, class: "px-4 py-2 bg-purple-600 text-white rounded-4xl hover:bg-purple-700 transition" %>
            <%= link_to "Log in", new_user_session_path, class: "px-4 py-2 bg-transparent border border-purple-600 text-purple-600 rounded-4xl hover:bg-purple-50 transition" %>
          </div>
        <% end %>
      </div>
    </div>
  </nav>