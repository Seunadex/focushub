<%= turbo_frame_tag "habits" do %>
  <div class="space-y-4 grid">
    <% habits.each do |habit| %>
      <%= turbo_frame_tag "habit_#{habit.id}" do %>
        <%= render 'habits/habit', habit: habit, extra_class: "" %>
      <% end %>
    <% end %>
  </div>
  <% if habits.empty? %>
    <div class="text-center text-gray-500 mt-6">
      <p class="text-gray-500">No active habits found.</p>
    </div>
  <% end %>
  <span class="text-gray-500 text-sm"><%= pagy_info(pagy).html_safe %></span>
  <% if pagy.pages > 1 %>
    <%= pagy_nav(pagy, base_path: dashboard_path, turbo_frame: "habits", page_param_name: :habits_page) %>
  <% end %>
<% end %>
